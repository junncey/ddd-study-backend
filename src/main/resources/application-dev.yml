# 开发环境配置

# 服务器配置 - 绑定所有网络接口
server:
  address: 0.0.0.0

# CORS 配置 - 允许所有来源访问
CORS_ALLOWED_ORIGINS: "*"

# CSP 配置 - 允许所有连接（仅开发环境）
CSP_CONNECT_SRC: "*"

# 安全头配置 - 开发环境可放宽
SECURITY_HEADERS_STRICT: false

# Swagger 启用
SWAGGER_ENABLED: true

# Druid 监控启用
DRUID_MONITOR_ENABLED: true
